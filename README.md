# 🏠 House Price Prediction

A machine learning project to predict house prices based on key features such as location, size, and amenities. This repository contains the dataset, preprocessing pipeline, trained models, and a notebook demonstrating the workflow.

---

## 📂 Project Structure

- **`housing_data.xls`** → Raw dataset containing housing features and prices.  
- **`house_price_predictions.ipynb`** → Jupyter Notebook with data exploration, preprocessing, model training, and evaluation.  
- **`main.py`** → Python script to load the trained model and make predictions.  
- **`house_price_model.pkl`** → Serialized trained model.  
- **`feature_scaler.pkl`** → Scaler used for feature normalization.  
- **`selected_features.pkl`** → Selected features used in training.  
- **`feature_names.json`** → Metadata of feature names.  
